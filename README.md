# LRBPF Project

This project is configured to deploy to GitHub Pages.

## Deployment Notes
- Uses Vite, React, and Tailwind CSS.
- Deployed via a custom GitHub Actions workflow (`deploy.yml`).
- Includes a `.nojekyll` file to bypass default GitHub Pages Jekyll processing, ensuring Vite's asset bundling works correctly.
